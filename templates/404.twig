{% set entry = craft.entries.section("notFound").limit(1).first() %}

{% extends "_layouts/_layout" %}

{% block _inline_css %}
    <style>
        {{ source ('404_critical.min.css', ignore_missing = true) }}
    </style>
{% endblock %}

{% block content %}
    
    <section>
        <div>
            <p>
                404 Not Found Error
            </p>
        </div>
    </section>

{% endblock %}

{% block _inline_js %}
{% endblock %}
